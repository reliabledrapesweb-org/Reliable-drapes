# Reliable Drapes - Implementation & Completeness Audit

This file replaces the old "incomplete tasks" list with a current audit of:

- implementation status (`Implemented`, `Partially Implemented`, `Not Implemented`)
- operational completeness (whether feature is controllable/manageable through admin/configuration)
- remaining gaps before production handover

Last updated: 2026-02-17 (post implementation pass)

---

## Status Legend

- `Implemented`: feature is present and functionally usable.
- `Partially Implemented`: feature exists but has missing pieces, broken links, UX inconsistency, or no admin/control path.
- `Not Implemented`: missing end-to-end.

---

## Requirement-by-Requirement Audit

### 1) Explore button -> E-catalogue, policy placement, B2B-only positioning

**Status**: `Implemented` (with governance note)

**What is implemented**
- Explore CTAs route to e-catalogue:
  - `src/components/features/home/HeroSection.tsx`
  - `src/components/shared/CTASection.tsx`
- Footer policy links were removed from footer.
- Shop page includes policy cards/links:
  - `/return-policy`, `/shipping-info`, `/faq`, `/privacy-policy`

**Completeness / control**
- No admin control needed for Explore routing itself.
- B2B-oriented wording has been applied on Home and supporting non-Shop sections.

**What was updated in this pass**
- Additional non-Shop wording shifted to B2B in:
  - `src/components/features/about/FounderSection.tsx`
  - `src/components/features/about/WhyChooseSection.tsx`
  - `src/components/features/about/FeaturesGrid.tsx`
  - `src/components/features/about/VisionMissionSection.tsx`
  - `src/app/exhibitions-events/page.tsx`
  - `src/app/store-locator/page.tsx`
  - `src/components/shared/PageHero.tsx`
- Previously completed B2B copy updates:
  - `src/app/layout.tsx`
  - `src/components/features/home/AboutSection.tsx`
  - `src/components/features/home/BenefitsSection.tsx`
- Policy pages exist and are accessible from Shop:
  - `src/app/return-policy/page.tsx`
  - `src/app/shipping-info/page.tsx`
  - `src/app/faq/page.tsx`

**Governance note**
- CMS-managed copy can still be edited by admins; content governance is needed to keep future updates aligned with B2B positioning.

---

### 2) Contact Us option visible on all pages

**Status**: `Implemented`

**What is implemented**
- Global nav includes Contact Us:
  - `src/lib/constants/navigation.ts`
  - used by `src/components/layout/Header.tsx`
- Footer also includes Contact Us:
  - `src/components/layout/Footer.tsx`
- Global floating contact shortcuts (WhatsApp + Contact page) on all non-admin pages:
  - `src/components/shared/GlobalContactButton.tsx`
  - wired in `src/components/layout/LayoutContent.tsx`

**Completeness / control**
- Structural nav coverage plus global quick-contact coverage are in place for all non-admin pages.

---

### 3) Exhibitions & Events section, remove Collection, add to footer, menu/footer placement

**Status**: `Implemented` (with legacy model note)

**What is implemented**
- Menu item exists:
  - `src/lib/constants/navigation.ts`
- Footer item exists:
  - `src/components/layout/Footer.tsx`
- Public exhibitions page is now an image-first gallery (event/exhibition photos):
  - `src/app/exhibitions-events/page.tsx`
- Exhibitions server actions exist:
  - `src/lib/actions/exhibitions.ts`
- Exhibitions admin page supports photo-centric management:
  - `src/app/admin/exhibitions/page.tsx`
  - linked in `src/components/admin/AdminSidebar.tsx`
- Admin now supports both:
  - image upload (via `FileUpload`)
  - manual image URL entry fallback
- Legacy collection routes are retired via redirects:
  - `src/app/collections/page.tsx` -> `/exhibitions-events`
  - `src/app/collections/[slug]/page.tsx` -> `/exhibitions-events`
  - `src/app/admin/collections/page.tsx` -> `/admin/exhibitions`
- Breadcrumb handling now reflects Exhibitions and no longer exposes Collections:
  - `src/components/shared/Breadcrumb.tsx`

**Completeness / control**
- Exhibitions are operationally complete for the current business requirement:
  image gallery on public page with admin add/edit/delete/publish controls and upload support.

**Legacy note**
- Internal collection data model/actions still exist in `src/lib/actions/products.ts` for existing backend relationships.
- This no longer affects public/admin navigation or user-facing menu/footer requirements.

**Placement recommendation**
- Keep current placement:
  - Header main nav: after `E-catalogue`, before `Store Locator`
  - Footer: `Company` column

---

### 4) Video integration near Explore area

**Status**: `Implemented`

**What is implemented**
- Home page includes `VideoSection` after hero:
  - `src/app/page.tsx`
- Video supports YouTube/upload and is DB-driven:
  - `src/components/features/home/VideoSection.tsx`
  - `src/lib/actions/site-settings.ts`

**Completeness / control**
- Fully controllable from admin Site Settings tab:
  - `src/app/admin/settings/page.tsx` (`hero_video_enabled`, `hero_video_url`, `hero_video_type`)
  - supports both uploaded video files and URL input fallback for upload mode

---

### 5) Store locator India map + store integration

**Status**: `Implemented`

**What is implemented**
- India map embed is present:
  - `src/app/store-locator/page.tsx`
- Dynamic store listing exists via actions.

**Completeness / control**
- Admin CRUD for stores exists:
  - `src/app/admin/stores/page.tsx`
  - `src/lib/actions/stores.ts`

---

### 6) Google Reviews integration

**Status**: `Implemented` (with control note)

**What is implemented**
- Google Reviews section added on Home with provided review screenshots:
  - `src/components/features/home/GoogleReviewsSection.tsx`
  - integrated in `src/app/page.tsx`
- Review screenshots stored in:
  - `public/images/google-reviews/review-1.jpg`
  - `public/images/google-reviews/review-2.jpg`
  - `public/images/google-reviews/review-3.jpg`
  - `public/images/google-reviews/review-4.jpg`
  - `public/images/google-reviews/review-5.jpg`

**Completeness / control**
- Integrated and visible to users.
- Current implementation is static (image-based) and not admin-configurable yet.

---

### 7) Remarketing / follow-up messages (retargeting)

**Status**: `Not Implemented`

**What exists**
- No active implementation for:
  - ad pixels
  - abandoned cart automation
  - push/web notification retargeting

**Completeness / control**
- No admin controls or automation pipeline present.

---

### 8) Offers & coupons in Shop

**Status**: `Implemented` (with scope note)

**What is implemented**
- Shop fetches and displays active coupons:
  - `src/app/shop/page.tsx`
  - `src/lib/actions/coupons.ts` (`getActiveCoupons`)
- Coupons admin CRUD page now exists:
  - `src/app/admin/coupons/page.tsx`
  - linked in `src/components/admin/AdminSidebar.tsx`
- Product-level offer visibility added on shop cards:
  - `src/components/features/shop/ShopProductGrid.tsx`
  - `src/components/features/shop/ShopProductCard.tsx`

**Completeness / control**
- Coupons are now controllable from admin and rendered in shop offer area.

**Remaining gaps**
- No coupon rules engine yet (e.g. category-specific targeting, stack rules, checkout auto-validation).

---

### 9) Search function fix

**Status**: `Implemented` (with QA note)

**What is implemented**
- Search modal with debounced query:
  - `src/components/layout/SearchModal.tsx`
- Shop page accepts query parameter and applies client filtering:
  - `src/app/shop/page.tsx`
- Search UX was improved:
  - Enter key in search modal now routes to shop search results
  - URL query sync in shop now updates for search/category set and clear flows
  - SKU matching added in shop client-side search filtering
  - Query normalization (`trim`) added for stable matching behavior
  - Modal close/reset behavior was hardened for repeat searches
  - Category-level server filter now includes SKU matching:
    - `src/lib/actions/products.ts` (`getProductsByCategory`)

**QA note**
- Functional fixes are implemented in code; final business validation should still be completed through UAT scenarios.

---

### 10) Company details + social media linking

**Status**: `Implemented` (with content dependency)

**What is implemented**
- Footer reads company details + social links from site settings:
  - `src/components/layout/Footer.tsx`
  - `src/lib/actions/site-settings.ts`

**Completeness / control**
- Admin-manageable in Site Settings:
  - `src/app/admin/settings/page.tsx` (`social_*`, `company_*`)

**Remaining gaps**
- Requires final business content to be populated in admin.

---

### 11) Ensure everything updated and tested before handover

**Status**: `Partially Implemented`

**What exists**
- Test framework and multiple specs are present.
- Validation executed for this implementation pass:
  - `npx eslint` (changed production files)
  - `npx tsc --noEmit`
  - Attempted targeted Vitest run is still blocked in this sandbox by `spawn EPERM`

**Remaining gaps**
- No evidence of full end-to-end regression over the business requirement set.
- Needs final UAT checklist and verification run after remaining items are completed.

---

### 12) Shop section "Coming Soon" message

**Status**: `Implemented`

**What is implemented**
- Conditional "Coming Soon" flow exists:
  - `src/app/shop/page.tsx`

**Completeness / control**
- Admin-controlled via Site Settings:
  - `shop_enabled`
  - `coming_soon_message`
  - in `src/app/admin/settings/page.tsx`

---

### 13) Footer policy content only in Shop

**Status**: `Implemented`

**What is implemented**
- Footer policy links removed.
- Policy links shown in Shop.
- Supporting pages implemented:
  - `src/app/return-policy/page.tsx`
  - `src/app/shipping-info/page.tsx`
  - `src/app/faq/page.tsx`

---

### 14) Remove premium/B2C promotional wording from Home

**Status**: `Implemented`

**What is implemented**
- Home/global promotional copy has been updated away from B2C-oriented "premium materials" style wording:
  - `src/components/features/home/AboutSection.tsx`
  - `src/components/features/home/BenefitsSection.tsx`
  - `src/app/layout.tsx`

---

## Admin/Control Completeness Summary

### Fully controllable today
- Site-wide settings (shop enable, coming soon text, hero video, social links, company details):
  - `src/app/admin/settings/page.tsx`
  - `src/lib/actions/site-settings.ts`
- Stores:
  - `src/app/admin/stores/page.tsx`
  - `src/lib/actions/stores.ts`

### Fully controllable today (newly completed)
- Exhibitions:
  - `src/lib/actions/exhibitions.ts`
  - `src/app/admin/exhibitions/page.tsx`
- Coupons:
  - `src/lib/actions/coupons.ts`
  - `src/app/admin/coupons/page.tsx`

### Not controllable / not implemented
- Google Reviews admin control (currently static image integration)
- Remarketing and retargeting automation

---

## Priority Next Actions

1. Execute full UAT regression and sign-off checklist for handover (all non-deferred requirements).
2. Populate final company details/social links content in admin settings.
3. Implement Google Reviews and remarketing strategy when scope is reopened.
